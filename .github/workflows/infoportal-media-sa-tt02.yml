name: infoportal-media-sa-tt02 deploy

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/infoportal-media-sa-tt02.yml
      - infrastructure/altinn-portaler-test/infoportal-media-sa-tt02/**
      - infrastructure/modules/infoportal-media-sa/**
  pull_request:
    branches:
      - main
    paths:
      - .github/workflows/infoportal-media-sa-tt02.yml
      - infrastructure/altinn-portaler-test/infoportal-media-sa-tt02/**
      - infrastructure/modules/infoportal-media-sa/**
  workflow_dispatch:
    inputs:
      log_level:
        required: true
        description: Terraform Log Level
        default: ERROR
        type: choice
        options:
          - TRACE
          - DEBUG
          - INFO
          - WARN
          - ERROR

env:
  TF_STATE_NAME: infoportal-media-sa-tt02.tfstate
  WORKING_DIRECTORY: ./infrastructure/altinn-portaler-test/infoportal-media-sa-tt02
  ARM_CLIENT_ID: ${{ secrets.AZURE_CLIENT_ID_ALTINN_PORTALER_TEST }}
  ARM_SUBSCRIPTION_ID: ${{ secrets.AZURE_SUBSCRIPTION_ID_ALTINN_PORTALER_TEST }}

permissions:
  id-token: write
  contents: write
  pull-requests: write

jobs:
  plan:
    name: Plan
    environment: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Terraform Plan
        uses: Altinn/altinn-platform/actions/terraform/plan@012d8205ebe0d9207a84e8ac8d586cb2ef83ac1a # main
        with:
          working_directory: ${{ env.WORKING_DIRECTORY }}
          oidc_type: environment
          oidc_value: test
          arm_client_id: ${{ env.ARM_CLIENT_ID }}
          arm_subscription_id: ${{ env.ARM_SUBSCRIPTION_ID }}
          tf_state_name: ${{ env.TF_STATE_NAME }}
          tf_log_level: ${{ github.event.inputs.log_level || 'ERROR' }}
          gh_token: ${{ secrets.GITHUB_TOKEN }}

  deploy:
    name: Deploy
    environment: test
    if: github.ref == 'refs/heads/main'
    needs: plan
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Terraform Apply
        uses: Altinn/altinn-platform/actions/terraform/apply@012d8205ebe0d9207a84e8ac8d586cb2ef83ac1a # main
        with:
          working_directory: ${{ env.WORKING_DIRECTORY }}
          oidc_type: environment
          oidc_value: test
          arm_client_id: ${{ env.ARM_CLIENT_ID }}
          arm_subscription_id: ${{ env.ARM_SUBSCRIPTION_ID }}
          tf_state_name: ${{ env.TF_STATE_NAME }}
          tf_log_level: ${{ github.event.inputs.log_level || 'ERROR' }}
          gh_token: ${{ secrets.GITHUB_TOKEN }}
